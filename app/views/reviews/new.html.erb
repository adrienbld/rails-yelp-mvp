<%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :content %>
 <!--    <%= f.input :rating %> -->
    <%= f.input :rating, collection: [0,1,2,3,4,5], class: 'form-control' %>
    <%= f.submit "Send Review", class: "btn btn-primary" %>

<% end %>

